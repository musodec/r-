# Backup files + the executable r~ build locally
*~

# Test directory
testdir/

# Exports from DEV.org
DEV.md
DEV.html

# Binaries for programs and plugins
r~.exe

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out
